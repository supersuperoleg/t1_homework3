spring:
  application:
    name: homework
  datasource:
    url: jdbc:postgresql://localhost:5433/mydb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

annotation-logger:
  enabled: true
  annotation-log-level: ERROR


